# SQF 文件括号嵌套修复脚本使用说明

## 概述

`fix_sqf_brackets.py` 是一个自动化脚本，用于检测和修复 SQF 文件中的括号嵌套问题。

## 功能特性

- ✅ **括号匹配检查**: 检测圆括号 `()`、方括号 `[]`、花括号 `{}` 是否匹配
- ✅ **括号自动修复**: 自动修复括号不匹配问题（未闭合、意外闭合、类型不匹配）
- ✅ **重复 if 嵌套检测**: 自动发现并修复重复的 if 语句嵌套
- ✅ **多编码支持**: 自动处理 UTF-8、Latin-1、CP1252 等多种编码
- ✅ **自动备份**: 修改前自动备份源文件
- ✅ **试运行模式**: 可预览修改而不实际应用
- ✅ **详细报告**: 生成 Markdown 格式的修复报告

## 使用方法

### 基本用法

```bash
# 试运行模式（推荐先运行，查看会修复哪些问题）
python fix_sqf_brackets.py --dry-run

# 实际应用修复
python fix_sqf_brackets.py
```

### 高级用法

```bash
# 指定项目根目录
python fix_sqf_brackets.py --project-root addons/mcc_sandbox_mod

# 指定备份目录
python fix_sqf_brackets.py --backup-dir my_backups

# 组合使用
python fix_sqf_brackets.py \
    --project-root addons/mcc_sandbox_mod \
    --backup-dir backups \
    --dry-run
```

## 参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--project-root` | 项目根目录 | `addons/mcc_sandbox_mod` |
| `--backup-dir` | 备份目录 | `backups` |
| `--dry-run` | 试运行模式（不修改文件） | `False` |

## 检测的问题类型

### 1. 括号不匹配

**问题类型**:
- **未闭合括号** (`unclosed`): 缺少闭合括号
- **意外闭合括号** (`unexpected_closing`): 多余的闭合括号
- **括号类型不匹配** (`mismatch`): 开括号和闭括号类型不匹配

**问题示例**:
```sqf
if (condition) then {
    // 缺少闭合括号
```

**修复**: 脚本会自动：
- 添加缺失的闭合括号
- 删除多余的闭合括号
- 替换不匹配的括号类型

### 2. 重复的 if 嵌套

**问题示例**:
```sqf
if (!(isNull _requestor)) then {
if (!(isNull _requestor)) then {
    [[netid _requestor,_requestor], "shoutS5"] remoteExec ["MCC_fnc_globalSay3D", 0, false];
};
};
```

**修复后**:
```sqf
if (!(isNull _requestor)) then {
    [[netid _requestor,_requestor], "shoutS5"] remoteExec ["MCC_fnc_globalSay3D", 0, false];
};
```

## 工作流程

1. **扫描文件**: 递归查找所有 `.sqf` 文件
2. **分析问题**: 检测括号匹配和重复 if 嵌套
3. **备份文件**: 在修改前自动备份
4. **应用修复**: 修复发现的问题
5. **生成报告**: 创建详细的修复报告

## 输出文件

- `sqf_bracket_fix_report.md`: 详细的修复报告，包含：
  - 统计信息
  - 每个文件的问题列表
  - 原始代码和修复说明

## 示例输出

```
============================================================
SQF 文件括号嵌套修复脚本
============================================================
[2025-11-18 13:00:00] [INFO] 模式: 试运行（不会实际修改文件）
[2025-11-18 13:00:00] [INFO] 开始扫描 SQF 文件...
[2025-11-18 13:00:00] [INFO] 找到 909 个 SQF 文件
[2025-11-18 13:00:05] [INFO] 正在分析: 100/909 (11%)
[2025-11-18 13:00:10] [INFO] 正在分析: 200/909 (22%)
...
[2025-11-18 13:01:00] [INFO] 分析完成，发现 5 个文件需要修复
[2025-11-18 13:01:00] [INFO] 开始修复 5 个文件...
============================================================
统计信息:
  总文件数: 909
  有问题文件数: 5
  括号不匹配数: 0
  重复 if 嵌套数: 8
  已修复文件数: 5
  错误数: 0
============================================================
```

## 注意事项

1. **备份**: 脚本会自动备份所有修改的文件到 `backups` 目录
2. **试运行**: 建议先使用 `--dry-run` 模式查看会修复哪些问题
3. **编码**: 脚本会自动尝试多种编码格式读取文件
4. **性能**: 扫描大量文件可能需要一些时间，脚本会显示进度

## 故障排除

### 问题: 编码错误

**原因**: 文件使用了非标准编码

**解决**: 脚本会自动尝试多种编码，如果都失败会跳过该文件并记录错误

### 问题: 修复后代码不正确

**原因**: 某些特殊情况可能无法正确识别

**解决**: 
- 检查备份文件
- 查看报告了解具体修复内容
- 手动调整修复结果

### 问题: 脚本运行缓慢

**原因**: 文件数量多，分析需要时间

**解决**: 
- 脚本会显示进度（每100个文件）
- 可以限制扫描范围（修改 `project_root` 参数）

## 技术细节

### 括号匹配算法

- 使用栈结构跟踪括号嵌套
- 忽略字符串和注释中的括号
- 支持单行注释 `//` 和多行注释 `/* */`

### 重复 if 检测

- 使用正则表达式匹配 if 语句
- 比较条件表达式（忽略空白差异）
- 跟踪花括号嵌套以确定作用域

## 许可证

本脚本遵循项目的许可证。

